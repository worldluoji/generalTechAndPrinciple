1. git pull的时候，报错：The following untracked working tree files would be overwritten by checkout
解决方法： 执行 git clean -d -fx 即可。

可能很多人都不明白-d，-fx到底是啥意思，其实git clean -d -fx表示：删除 一些 没有 git add 的 文件；
-n 显示将要删除的文件和目录；
-x -----删除忽略文件已经对git来说不识别的文件
-d -----删除未被添加到git的路径中的文件
-f -----强制运行

建议使用 git clean -d -f